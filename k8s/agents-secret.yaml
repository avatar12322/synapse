# Template — do NOT commit real values.
# Populate via: kubectl create secret generic agents-secret --from-env-file=.env -n synapse
# Or use Sealed Secrets / External Secrets Operator for GitOps.
apiVersion: v1
kind: Secret
metadata:
  name: agents-secret
  namespace: synapse
type: Opaque
stringData:
  GOOGLE_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  DATABASE_URL: ""
  REDIS_URL: ""
